#include "main.h"

/**
*print alphabets, in lowercase, followed by a new line.
*
*Return: Always return 0
*/
int main(void)
{
	char beta;

	for (beta = 'a'; beta <= 'z'; beta++)

		_putchar(beta);
	_putchar('\n');
	return (0);
}
